# Environment Variables Template
# Copy this file to .env.local and fill in your values
# .env.local is gitignored and should NEVER be committed

# ======================
# ADMIN AUTHENTICATION
# ======================

# Admin panel password - Use a strong, unique password
# Generate with: openssl rand -base64 24
# Or use a password manager
ADMIN_PASSWORD=change-this-to-a-strong-password

# Optional: NextAuth secret (if using NextAuth.js)
# Generate with: openssl rand -base64 32
# NEXTAUTH_SECRET=your-nextauth-secret-here

# Optional: NextAuth URL (for production)
# NEXTAUTH_URL=https://your-production-domain.com

# ======================
# SECURITY (Optional)
# ======================

# Comma-separated list of allowed IPs for admin access
# Example: 203.0.113.1,198.51.100.42
# Leave empty to allow all IPs
# ADMIN_ALLOWED_IPS=

# ======================
# FUTURE: DATABASE
# ======================

# When migrating from JSON to database, add connection strings here:
# DATABASE_URL=postgresql://user:password@host:port/database
# Or for MongoDB:
# MONGODB_URI=mongodb+srv://user:password@cluster.mongodb.net/database

# ======================
# DEPLOYMENT NOTES
# ======================

# For AWS Amplify:
# Set these in Console → App → Environment Variables

# For Azure Static Web Apps:
# Set these in Portal → Configuration → Application Settings

# For Vercel:
# Set these in Dashboard → Project → Settings → Environment Variables

# NEVER commit .env.local or production credentials to git!
